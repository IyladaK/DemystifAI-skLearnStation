For VsCode:
- install jupyter extension
- install python extension
- Create a python virtual environment (venv) and use it as the kernel
- if it prompts you to install ipykernel, do so

Required Packages:<br>
pip install numpy<br>
pip install matplotlib<br>
pip install scikit-learn<br>
pip install joblib<br>
pip install pygame<br>
pip install opencv-python<br>
pip install pandas<br>
pip install mediapipe<br>

Computer Vision Full Course (for those interested in mediapipe):
- https://www.youtube.com/watch?v=01sAkU_NvOY&t=4979s
